import types from './types';


const INITIAL_STATE = {
  
}


const shipwayReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {

    default: return state;
  }
}

export default shipwayReducer;